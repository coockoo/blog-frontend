mutation CreateArticle($title: String!, $outline: String!, $slug: String!, $body: String!) {
  createArticle(title: $title, outline: $outline, slug: $slug, body: $body) {
    id
    title
    outline
    slug
    body
    isPublished
    lastPublishedAt
    createdAt
    updatedAt
  }
}
